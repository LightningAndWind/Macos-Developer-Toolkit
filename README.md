# Macos-Developer-Toolkit (devkit)

面向 macOS 开发者的本地化工具集，追求**精简、快速、低内存占用**。将日常开发中高频使用的工具聚合到一个 App 内，用标签页方式并行工作，减少在多个重量级应用之间切换的开销。

> 内存太贵，只能用精简应用了。

## 技术栈

- **前端**：SwiftUI（AppKit 混合，终端等场景用 `NSViewRepresentable` 封装）
- **终端**：SwiftTerm
- **SSH / SFTP**：SwiftNIO + NIOSSH（密码认证进程内实现），私钥认证走系统 `/usr/bin/ssh` 子进程
- **数据存储**：SQLite（HTTP 历史、集合、SSH 连接配置等）
- **依赖管理**：Swift Package Manager
- **工程**：Xcode 16 同步文件夹组，新增 Swift 文件自动纳入编译

## 应用外壳

- **标签页管理**：一个标签 = 一个工具实例，支持多实例并行、分组折叠、自定义标题、双击重命名、拖拽排序（Chrome 风格）。
- **窗口分离**：支持的标签可拖出为独立窗口。
- **会话快照持久化**：App 重启后自动还原已打开的标签及各工具内部状态（请求内容、编辑文本、视图模式、连接配置绑定、脏标记等），通过统一的 `sessionStateData()` / `restoreSessionState(_:)` 钩子实现。
- **外观模式**：跟随系统 / 浅色 / 深色三档切换，全局生效（含终端配色与毛玻璃材质）。
- **工具注册协议**：所有工具实现统一的 `DevkitTool` 协议，Shell 只负责装配，新增工具只需在注册表加一行即可插拔接入。

## 内置工具

### HTTP 请求（网络）

构造并发送 HTTP 请求，查看响应与历史记录。

- 完整的请求编辑器：方法、URL、Headers、Body 等
- 响应查看器，支持格式化展示与复制
- **历史记录**：基于 SQLite 存储完整请求与响应，支持关键字搜索、重发、删除
- **集合管理**：请求可按多级文件夹归类保存（类似 Postman），支持 ⌘S 原地更新 / 另存为，脏标记 `*` 提示未保存改动
- **复制为 cURL**：将当前请求导出为 cURL 命令
- 删除已保存请求时，通过外键精确级联清理其自身发出的历史记录

### SSH 终端（终端）

本地终端与 SSH 会话，连接配置按文件夹归类管理。

- **本地终端**：新标签默认进入 `~`
- **双引擎远程连接**：
  - 密码认证 → 进程内 NIOSSH，自动登录，主机指纹走应用内 TOFU（首次使用信任）
  - 私钥认证 → 系统 `ssh` 子进程，原生支持 RSA、口令与 `known_hosts`
- **连接配置库**：profile 存 SQLite，多级文件夹归类管理
- 断开后可一键重新连接；会话还原后置为「已断开」由用户主动点击重连，不自动联网
- 终端历史输出用有上限的 scrollback 回滚缓冲控制（当前 3000 行），内存有界

### SFTP 传输（终端）

左右两区各连一台主机，多选文件或文件夹双向互传。

- 一个标签 = 左右两个文件面板，各自独立选择 SSH 连接（复用 SSH 工具的连接配置库）
- 支持文件 / 文件夹多选，递归复制整个目录树
- 传输经本地临时文件中转（源下载 → 目标上传）
- **双引擎后端**：远端走 NIOSSH SFTP，本地侧走文件系统，同一协议抽象支持本地与远端互传
- 采用 request-id 并发流水线提升传输吞吐，实时显示整体字节进度与文件计数

### JSON 工具（数据）

文本 / 树形双视图编辑、格式化、校验、键排序。

- **文本视图**：语法高亮、行号、括号配对、自动缩进的完整编辑器（`NSTextView` 封装）
- **树形视图**：基于解析结果生成的只读导航树
- 格式化 / 压缩 / 校验 / 键排序（升序、可递归）
- 手写递归下降解析器，提供精确行列与尾逗号、单引号等错误提示
- 无长期存储，但标签内容随会话快照自动留存，App 重启后原样还原

## 项目结构

```
devkit/devkit/
├── Core/            # 数据、设计令牌、模型、偏好设置
│   └── Models/      # 工具协议、注册表、标签/分组模型
├── Shell/           # 应用外壳：状态管理、标签/分组视图
├── Tools/           # 各工具（HTTP / SSH / SFTP / JSON / Shared）
├── Utilities/       # 光标样式、面板、窗口配置等辅助
├── ContentView.swift
└── devkitApp.swift
```

每个工具内部按 `Logic`（协议/客户端/存储）、`Models`（数据模型）、`Views`（SwiftUI 视图）分层组织。

## 构建

使用 Xcode 16 打开 `devkit/devkit.xcodeproj` 编译运行，或通过 `xcodebuild` 构建打包 dmg。
